{{!-- 撰寫搜尋架構 --}}
<div class="container search-bar">
  <div class="search-bar-outer">
    <div class="search-bar-inner">
      <form action="/search">
        <h4 class="mb-2">尋找餐廳</h4>
        <div class="input-group">
          <input type="text" class="form-control" name="keyword" value="{{keyword}}" placeholder="輸入餐廳、分類" aria-label="Restaurant Name...">
          <div class="input-group-append">
            <button type="submit" id="search-button" class="btn btn-major">
              搜尋
              <i class="fas fa-search"></i>
            </button>
          </div>
        </div>
      </form>
    </div>
  </div>
</div>


{{!-- 撰寫餐廳清單圖卡 --}}
<div class="container mt-5">
  <div class="wrapper">
    <div class="col col-md-10 mx-auto">
      <div class="row row-cols-sm-3">
        {{#each restaurants}}
          <a href="/restaurants/{{this.id}}" class="text-secondary">
            <div class="card mb-3">
              <img src="{{this.image}}" alt="{{this.name}}" class="card-img-top">
              <div class="card-body p-3">
                <h6 class="card-title mb-1">{{this.name}}</h6>
                <div class="restaurant-category mb-1">
                  <i class="fas fa-utensils pr-2"></i>
                  {{this.category}}
                </div>
                <span class="text-secondary badge badge-pill badge-danger font-weight-normal">
                  {{this.rating}}
                  <i class="fas fa-star fa-xs"></i>
                </span>
              </div>
            </div>
          </a>
        {{/each}}
      </div>
    </div>
  </div>
</div>



